# Hyperparameters

Hyperparameters are parameters outside the model, in contrast to weights and
biases which are inside the model. Examples of hyperparameters:
- Number of epochs
- Batch size
- Learning rate
